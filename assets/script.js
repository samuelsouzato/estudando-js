let dolar = 5.1

let usdInput = document.quertSelector("#usd")
let brlInput = document.quertSelector("#brl")

usdInput.addEventListener("keyup", () => {

})

brlInput.addEventListener("keyup", () => {
    
})

usdInput.value = "1000,00"
convert("usd-to-brl")

//Funções

function convert (type){
    if(type == "usd-to-brl") {

    }   
    if(type == "brl-to-usd") {
        
    }  
}